server {
    listen 80;
    server_name radars.alwaysmoveforward.com local.radars.alwaysmoveforward.com;

    root /usr/share/nginx/html;
    index index.html;

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri $uri/ /index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}

server {
    listen 80;
    server_name manage.radars.alwaysmoveforward.com local.manage.radars.alwaysmoveforward.com;

    root /usr/share/nginx/html;
    index ManageRadars.html;

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri $uri/ /ManageRadars.html;
    }

    location / {
        try_files $uri $uri/ /ManageRadars.html;
    }
}

server {
    listen 80;
    server_name admin.radars.alwaysmoveforward.com local.admin.radars.alwaysmoveforward.com;

    root /usr/share/nginx/html;
    index AdminSite.html;

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri $uri/ /AdminSite.html;
    }

    location / {
        try_files $uri $uri/ /AdminSite.html;
    }
}
